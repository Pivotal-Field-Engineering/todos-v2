---
applications:
- name: todos-api
  memory: 1G
  domain: apps.internal
  path: target/todos-api-1.0.0.SNAP.jar
  buildpack: java_buildpack_offline
  services:
  - todos-config
  - todos-registry
  env:
    TRUST_CERTS: api.sys.retro.io
    SPRING_PROFILES_ACTIVE: direct

